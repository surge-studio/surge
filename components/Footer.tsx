import { Logomark } from './Logomark';
import type { FC } from 'react';

export const Footer: FC = () => (
  <footer className="relative flex h-[300px] items-center justify-center">
    <Logomark />
  </footer>
);
